/**
 * Ramp (30/60/90) overview. Ties to the quota plan and the onboarding wizard;
 * wired in Phase 2/3. Placeholder until real data lands.
 */
export function RampPage() {
  return (
    <div className="flex flex-col gap-4">
      <h1 className="text-2xl font-bold tracking-tight">Ramp</h1>
      <p className="text-muted-foreground">
        30 / 60 / 90 flight plan. Wired in Phase 2 once quota plans and the onboarding wizard exist.
      </p>
    </div>
  );
}
