# components/charts — chart primitives

Reserved for dashboard / forecast / weekly-review charts (quota progress rail,
risk breakdown, forecast vs target). No current use case → intentionally not
created as code (working rule: no empty abstractions). Add primitives here
(dependency choice TBD, likely SVG or Recharts) when weekly-review or forecast
lands in Phase 3.
