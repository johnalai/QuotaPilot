// Placeholder — versioned, typed AI prompt templates land with the discovery-call
// coach (deferred until the core workflow works). index.ts will export a registry
// mapping feature → { version, inputSchema, outputSchema, render }.
export {};
